[project]
name = "penpaw-pipeline"
version = "0.1.0"
description = "PenPaw three-stage neural-symbolic pipeline (M1: mock implementations)"
requires-python = ">=3.11,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["penpaw_pipeline"]
